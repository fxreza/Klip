# Task 3A - Lock / protect UX (Sonnet 5)

WORKTREE: (set at launch). Do NOT commit. Do NOT touch the main checkout.

Read first: `docs/plan/PLAN.md` (requirements: lock any clip; folder clips locked by default; locked clips immune to history cleanup, select-delete, right-click delete and the delete shortcut until unlocked), `docs/analysis/buffer.md` §3, then the code: `Models/ClipboardItem.swift` (`isLocked`, `isProtected`), `Services/ClipboardStore.swift` (`toggleLock`, `setLocked`, `delete` returns `Bool`/`DeleteResult`, `clear(keepProtected:)`), `Views/History/HistoryViewModel.swift`, `Views/History/PreviewPane.swift` (`actionIcons(for:)` - trash already `.disabled(item.isLocked)`), `Views/History/ClipRow.swift` (`trailingBadges` already shows `lock.fill`), `Views/History/MultiSelectionSummary.swift` (inline delete confirm), `Views/StatusBarController.swift` (`clearHistory` alert), `Views/SettingsView.swift` (History tab), `Views/Theme/Theme.swift` (`lockTint`), `Views/History/PromptCard.swift`.

Concurrency rules: three other agents edit `HistoryViewModel.swift` at the same time. Put your **stored state** in one block at the very end of the class body headed `// MARK: - 3A state` and your **methods** in a new file `Views/History/HistoryViewModel+Lock.swift` (`extension HistoryViewModel`). Do NOT edit `RowContextMenu.swift` (task 3D rewrites it and will include a Lock/Unlock item calling your `toggleLockSelection()`), `GlobalKeyMonitor.swift` (3E wires ⌘L to your `keyLock()`), `Sidebar.swift`, `ClipList.swift`, `ClipboardWatcher.swift`, `PasteController.swift`.

## Deliverables
1. View-model API (in the extension): `func toggleLockSelection()` (toggles lock on the selected item, or on all multi-selected: if any is unlocked -> lock all, else unlock all), `func keyLock()` (= toggleLockSelection), `func setLocked(_ locked: Bool, ids: Set<UUID>)`, and delete paths: every delete entry point in the view model (`deleteSelected`, the multi-selection confirm, any `delete(item)`) must go through one `performDelete(ids:)` that calls the store, reads the returned `deleted`/`skippedLocked`, keeps locked items selected, and shows a toast when `skippedLocked > 0`: "1 locked clip was not deleted - unlock it first" / "N locked clips were not deleted - unlock them first". Stored state: `@Published var toast: ToastMessage?` (`struct ToastMessage: Equatable { let text: String; let systemImage: String }`), auto-cleared after 3 s.
2. `Views/History/Toast.swift`: a small capsule toast (material, `Theme` tokens, `Font.klip(.caption)`) shown at the bottom of the content area over the list; add it to `HistoryContentView` as an overlay (one `.overlay(alignment: .bottom)` line - that is the only edit allowed in `HistoryContentView.swift`). Respect reduced motion.
3. `PreviewPane.actionIcons(for:)`: add a lock/unlock button (`lock.fill` / `lock.open`, tint `Theme.lockTint` when locked, `.help("Lock (⌘L)")`), placed before the trash; trash stays disabled when locked with `.help("Locked - unlock to delete")`. Multi-selection summary: show "N locked" in the counts line and, in the delete confirmation, "Delete M clips (N locked will be kept)".
4. Clear History (`StatusBarController.clearHistory`): alert text becomes "Clear history? Pinned, starred, tagged, locked and folder clips are kept." and after clearing show (via `NSAlert` or the toast if the window is open) "Cleared X clips; Y protected clips kept" using `DeleteResult`.
5. Settings > History tab: a short explanatory paragraph: "Locked clips can't be deleted until you unlock them. Clips inside folders are locked automatically. Pinned, starred, tagged, locked and folder clips never count toward the history limit."
6. Eviction is already lock-safe (store). Add tests `Tests/LockTests.swift`: view-model `performDelete` with a mixed selection keeps locked ids selected and sets the toast text; `toggleLockSelection` semantics; store `clear` keeps locked. Register in `TestRunner.swift` (suite list only).

## Verify
`scripts/gate.sh` green. Offscreen render (screen is locked) of the preview pane with a locked item selected and of the toast - a harness like the one in `/private/tmp/claude-501/-Users-sam-Claude-Code-clipboard-manager/3854505a-128a-45e2-b039-51ef49965b3a/scratchpad/harness/` (RenderHarness.swift + render.sh, from task 2A - reuse it, adapt paths to your worktree). Remove `build/`.

Owns: `Views/History/HistoryViewModel.swift` (3A state block only), `Views/History/HistoryViewModel+Lock.swift`, `Views/History/Toast.swift`, `Views/History/HistoryContentView.swift` (one overlay line), `Views/History/PreviewPane.swift` (action cluster + locked help), `Views/History/MultiSelectionSummary.swift`, `Views/StatusBarController.swift` (clear alert), `Views/SettingsView.swift` (History tab text), `Tests/LockTests.swift`, `Tests/TestRunner.swift` (suite list).
Return: summary, API names, test output, render descriptions, `git status --short`.
