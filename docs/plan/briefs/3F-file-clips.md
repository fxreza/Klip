# Task 3F - File clips: capture, store, paste, save to disk, QuickLook previews (Sonnet 5, Opus review later)

WORKTREE: (set at launch). Do NOT commit. Do NOT touch the main checkout.

Read first: `docs/plan/PLAN.md` (D6: copy into storage up to a cap from 1/5/10/50/100/500 MB/Unlimited + custom, default 50 MB; above cap = reference + bookmark; "save copied images to disk" extended to any file type; thumbnails/previews for pdf/csv/txt etc. if easy), `docs/analysis/buffer.md` §3 (Save image to disk, OCR) and change map E, `docs/analysis/clipfield.md` §6 (file capture), then code: `Models/FileAttachment.swift`, `Models/ClipboardItem.swift` (`.file`, `ClipboardItem.file(attachment:sourceApp:)`), `Services/ClipboardWatcher.swift` (the `NSFilenamesPboardType` branch that today only handles a single image file, and 3C's kind assignment), `Services/ClipboardStore.swift` (`files/` dir, `fileURLs(for:)` - provisional from 2A, you own the final version, `itemSize`, `deleteAssociatedFiles`, `saveImage`), `Services/PasteController.swift` (`copyToClipboard/paste/pasteMultiple` `.file` branches, `saveImageToDisk`, `downloadAllImages` in the view model), `Views/History/PreviewPane.swift` (`fileBody(_:)`), `Views/History/ClipRow.swift` (`badge` file branch), `Views/History/ItemKindDisplay.swift` (`fileIcon(store:)`), `Views/History/MultiSelectionSummary.swift` (Download All), `Views/SettingsView.swift`, `Services/SettingsManager.swift`.

Concurrency: other agents edit `HistoryViewModel.swift`, `PreviewPane.swift` (3A: action cluster), `MultiSelectionSummary.swift` (3A: counts/delete text), `ClipRow.swift` (3B: drag helper). Keep your edits to the named functions/regions; stored view-model state in a block `// MARK: - 3F state`, methods in `Views/History/HistoryViewModel+Files.swift`.

## Deliverables
1. **Capture** (`ClipboardWatcher`): when the pasteboard has file URLs (`NSPasteboard.general.readObjects(forClasses: [NSURL.self], options: [.urlReadingFileURLsOnly: true])`), for ANY file type and count: keep today's behavior for a single image file (becomes an `.image` item) and otherwise create ONE `.file` item (`originalName` = first file name, `additionalNames` = the rest, `uti` via `UTType(filenameExtension:)`, `byteSize` = total). Copy policy: if total size <= cap (`SettingsManager.fileCopyCapMB`, 0 = unlimited) copy each file into `files/<uuid>/<name>` (`storedRelativePath` = `files/<uuid>` directory; keep original names; directories are copied recursively only if under the cap, else referenced), else store `referencePath` + `bookmark` (`URL.bookmarkData(options: .withSecurityScope...)` - app is not sandboxed, a plain bookmark is fine) and set `syncSkippedLarge`-style flag `isReference`. Hash = SHA-256 of path list + sizes + mtimes, so copying the same file twice dedupes. Source app as today. Kind `.file`.
2. **Store**: final `fileURLs(for:) -> [URL]` (stored copies, else resolved bookmark/reference path, skipping missing files), `fileIsMissing(item)`, `itemSize`, `deleteAssociatedFiles` removes `files/<uuid>/`. Cap setting: `fileCopyCapMB: Int` (key `files.copyCapMB`, default 50) + `fileCopyCapCustom` handling in the UI.
3. **Paste / copy** (`PasteController`): `.file` writes all URLs (`NSPasteboard.writeObjects([NSURL])` + a plain-text fallback of the paths, same as Finder) so pasting into Finder copies the files and into Mail/Slack attaches them; `pasteMultiple` with mixed items: files as URLs. Missing files -> toast via `HistoryViewModel.toast` (3A adds it; if absent in your worktree, add the same `ToastMessage`/`toast` property in your 3F state block with a `// 3A duplicate, resolve at merge` marker).
4. **Save to disk**: generalize `saveImageToDisk` into `saveToDisk(item:)`: images as today; text items save as `.txt` (plain) via `NSSavePanel`; file items: single -> `NSSavePanel` prefilled with the original name, multiple -> `NSOpenPanel` directory chooser then copy all. Multi-selection "Download All" becomes "Save All…" and handles images + files + texts into a chosen folder with unique names. Update the ⌘S help/legend text to "Save to Disk" (the action id is `saveToDisk` in `ShortcutAction`).
5. **Previews**: `Views/History/FilePreview.swift`: thumbnails via `QLThumbnailGenerator` (QuickLookThumbnailing, 38x38 and 240 pt), cached in an `NSCache` keyed by item id + size; `fileBody` in the preview pane shows a `QLPreviewView` (Quartz) for the first file when it exists (pdf, csv, txt, code, images, office docs all preview natively) with a fallback card (icon, name(s), size, "Reference - original file" badge, "Reveal in Finder"); text-like files (`public.plain-text`, `public.source-code`, csv, json, md) under 1 MB additionally offer "Show as text" using the chunked text view if that is simple - otherwise skip. `ClipRow.badge` uses the thumbnail when available, else the file icon. Link `QuickLookThumbnailing` and `Quartz` frameworks: add `-framework Quartz -framework QuickLookThumbnailing` to `scripts/build_local.sh`, `scripts/run_tests.sh`, `build_dmg.sh` (you own those flag lines).
6. **Settings**: Files section in the History tab (or a new "Files" tab if cleaner): cap picker (1, 5, 10, 50, 100, 500 MB, Unlimited, Custom… with a numeric field), note about disk use on Unlimited, and "Open storage folder in Finder".
7. Tests `Tests/FileClipTests.swift`: capture policy (under/over cap with a temp file), dedupe hash, `fileURLs` for stored and reference items, missing-file detection, `deleteAssociatedFiles` cleanup, save-all name uniquing helper. Register in `TestRunner.swift` (suite list only).

## Verify
`scripts/gate.sh` green. Live: `scripts/run_app.sh`, then put a real file on the pasteboard from a script (e.g. a Swift one-liner or `osascript -e 'set the clipboard to POSIX file "/path"'` - osascript clipboard setting does not need Accessibility) for a `.py`, a `.pdf` (create one with `cupsfilter` or copy any PDF under `/System/Library`) and an `.xlsx`-like file; confirm `history.json` has `.file` items and `files/<uuid>/` copies; quit. Offscreen-render the row and preview for a pdf using the 2A harness in `/private/tmp/claude-501/-Users-sam-Claude-Code-clipboard-manager/3854505a-128a-45e2-b039-51ef49965b3a/scratchpad/harness/`. Remove `build/`.

Owns: `Services/ClipboardWatcher.swift` (file branch), `Services/ClipboardStore.swift` (files functions), `Services/PasteController.swift` (file paste + save functions), `Services/SettingsManager.swift` (file cap keys), `Views/History/FilePreview.swift`, `Views/History/PreviewPane.swift` (`fileBody` only), `Views/History/ClipRow.swift` (badge file branch only), `Views/History/ItemKindDisplay.swift`, `Views/History/MultiSelectionSummary.swift` (Save All only), `Views/History/HistoryViewModel.swift` (3F state block), `Views/History/HistoryViewModel+Files.swift`, `Views/SettingsView.swift` (Files section), scripts/build flags, `Tests/FileClipTests.swift`, `Tests/TestRunner.swift` (suite list).
Return: summary, API, test output, what was verified live, `git status --short`.
