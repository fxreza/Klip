# Task 6A - Fixes from the user's first live test (Opus 5)

WORKTREE: (set at launch). Do NOT commit. Do NOT touch the main checkout or `~/Library/Application Support/Klip` (use `KLIP_DATA_DIR` temp dirs). The screen is now UNLOCKED and the user is around: you may use `screencapture -x` for verification, but do not drive the UI with synthetic mouse/keyboard events.

Crash reports: `~/Library/Logs/DiagnosticReports/Klip-2026-08-22-09*.ips` (3 identical): `QLPreviewView setPreviewItem` -> `_QLRaiseAssert` -> abort, called from `FileQuickLookView.updateNSView` during a SwiftUI update. Read `Views/History/FilePreview.swift`, `Views/History/PreviewPane.swift` (`fileBody`), `Views/History/HistoryContentView.swift`, `Views/History/ActionBar.swift`, `Views/History/ClipList.swift`, `Views/History/GlobalKeyMonitor.swift`, `Services/ShortcutManager.swift`, `Views/History/RowContextMenu.swift`, `Views/History/Sidebar.swift`, `Views/History/HistoryViewModel*.swift`, `Views/Settings/ShortcutsTab.swift`.

## Fixes (all required unless marked optional)
1. **Crash (user items 1 and 8)**: remove `QLPreviewView` entirely. `fileBody` renders a QuickLook *thumbnail* (`QLThumbnailGenerator`, size = pane width x 320 pt, scale = screen scale, cached like the row badge) above the fallback card (name(s), size, kind, "Reference"/"missing" badges, Reveal in Finder, Open). Never call QuickLook UI classes. Regression test: the preview for a file item renders via the harness without QuickLookUI linked (assert the symbol is absent from the binary: `nm build/... | grep -c QLPreviewView` == 0, add that check to `scripts/gate.sh`). Also audit every other `NSViewRepresentable.updateNSView` for work that can assert during layout.
2. **Favorite shortcut**: default for `.star` becomes **⌘F** (nothing else uses ⌘F; search is focused automatically). Users who already stored an override keep it. Update defaults table, tests, legend, help strings, CHANGELOG.
3. **Rename "star" -> "favorite" everywhere user-facing**: legend, context menu ("Favorite"/"Unfavorite"), preview buttons `.help`, Shortcuts tab label ("Favorite"), Settings text, toasts. Keep the `isBookmarked`/`.star` identifiers internal. Sidebar scope stays "Favorites".
4. **Remove scope cycling**: delete `nextScope`/`previousScope` actions from `ShortcutAction` (tolerate stored overrides for them by ignoring unknown keys when decoding), their key-monitor dispatch, view-model methods, legend entries and Shortcuts-tab rows. Sidebar clicking stays.
5. **Remove the blue Paste button** from the action bar (and its split menu). The "Always paste as plain text" toggle stays in Settings > General; the plain/rich alternate stays reachable via ⌥↩ and the context menu.
6. **Legend**: add "⌥↩ paste plain" (uses `ShortcutManager.displayString(for: .pastePlain)`) next to the paste entry; with the button gone there is room - the full tier should fit at the default window width.
7. **Layout (user item 5)**: the preview pane must be full panel height like the sidebar. Restructure `HistoryContentView`: `HStack { Sidebar | PanelResizer | middle column VStack { SearchBar; FilterChipBar; Divider; ClipList; Divider; ActionBar } | PanelResizer | PreviewPane (full height, its own top padding matching the search bar's) }`. Keep all resizing/persistence behavior.
8. **Scroll-to-selection (user item 7)**: keyboard navigation sometimes moved the highlight without scrolling. Make `ClipList` scroll on every `selectedID` change both synchronously and again on the next run loop (`DispatchQueue.main.async`) with `anchor: .center` only when the row is outside the visible range (track visible ids via `onAppear/onDisappear` or a `GeometryReader`-based visibility check) so it never fights the user's own scrolling; also re-scroll after `filteredItems` changes if the selection is off-screen.
9. **Keyboard navigation blink (user item 4, best-effort)**: when the selection changes via keys, apply the selection highlight without the spring animation (`withAnimation(nil)` / `.transaction { $0.animation = nil }` on key-driven changes; keep the animation for mouse clicks). Don't spend long on it; if it isn't a clean win, leave it and say so.
10. Bump `Info.plist` to 3.0.1 / build 9 and add a `## 3.0.1` CHANGELOG section listing these items.

## Verify
`scripts/gate.sh` green (with the new QLPreviewView symbol check). Run `scripts/run_app.sh`, copy a PDF file onto the pasteboard (`osascript -e 'set the clipboard to POSIX file "/System/Library/Desktop Pictures/Solid Colors/Stone.png"'` or any PDF under /System/Library), `notifyutil -p com.fxreza.klip.debug.show`, select it through the view-model harness or via a `KLIP_SELECT_FIRST=1` relaunch, and confirm no crash over 30 s plus a `screencapture -x` showing the thumbnail preview and the new layout (full-height preview, no Paste button, legend with "paste plain" and "favorite"). Kill the instance; remove `build/`.

Owns: `Views/**`, `Services/ShortcutManager.swift`, `Services/PasteController.swift` (only if the button removal needs it), `Info.plist`, `CHANGELOG.md`, `scripts/gate.sh` (symbol check), `Tests/**`.
Return: per-item status, screenshot description, gate output, `git status --short`.
