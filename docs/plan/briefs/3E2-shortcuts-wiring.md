# Task 3E part 2 - Table-driven key monitor + dynamic legend (Sonnet 5)

WORKTREE: (set at launch). Do NOT commit. Do NOT touch the main checkout.

Read first: `docs/plan/briefs/3E-custom-shortcuts.md` (part 1, done: `Models/KeyBinding.swift`, `Services/ShortcutManager.swift` with `ShortcutAction`, `action(for: NSEvent)`, `displayString(for:)`), then `Views/History/GlobalKeyMonitor.swift` (the hardcoded keycode switch and its gating: edit mode, tag input, prompts, search focus, NSTextView first responder for ⌘C), `Views/History/HistoryViewModel.swift` (entry points `keyUp/keyDown/keyEnter/keyEscape/keyDelete/keyCopy/keyPin/keyBookmark/keySaveImage/keyAddTag/keyEdit/keyTabComplete/keyNextScope/keyPrevScope/toggleSidebar/togglePreviewPane`), `Views/History/ActionBar.swift` (`legendItem(_:_:)`), `Views/History/PreviewPane.swift` (`.help("… (⌘P)")` strings), `Views/StatusBarController.swift` (shortcut label line), `Views/History/RowContextMenu.swift` (read only; 3D adds `keyboardShortcut`s later).

Concurrency: other agents add methods to `HistoryViewModel` in their own files (`+Lock.swift` from 3A: `keyLock()`; `+Folders.swift` from 3B: `keyNewFolder()`, `keyRenameFolder()`, `keyMoveToFolder()`; 3D later: plain paste/copy). Your worktree does not have them yet, so: put stored state in a `// MARK: - 3E state` block and methods in `Views/History/HistoryViewModel+Shortcuts.swift` containing **temporary stubs** for `keyLock`, `keyNewFolder`, `keyRenameFolder`, `keyMoveToFolder`, `keyPastePlain`, `keyCopyPlain`, `quickPaste(index:)` marked `// 3E-stub: replaced at merge by 3A/3B/3D` (stubs: `quickPaste(index:)` should be real - select the Nth visible item and paste; `keyPastePlain/keyCopyPlain` call the normal paste/copy for now). Do NOT edit `RowContextMenu.swift`, `Sidebar.swift`, `PreviewPane.swift` except the `.help` strings.

## Deliverables
1. `GlobalKeyMonitor`: replace the keycode switch with `ShortcutManager.shared.action(for: event)` -> `switch action` dispatching to the view-model methods. Preserve every gate exactly: fixed keys (↑↓⇧↑⇧↓ ↩ ⌥↩ ⇥ ⎋ ⌫) keep their current special cases (⌫ only when search is empty, ⌘⌫ delete, Tab completion only during tag input, ↩ commits tag input first, Esc chain); ⌘C defers to an `NSTextView` first responder as today; when a prompt or edit mode is active only the keys that previously worked keep working. Unbound/unknown events fall through (return the event) exactly as before.
2. Legend: `ActionBar` builds its items from `ShortcutManager.displayString(for:)` for: paste, pastePlain, copy, pin, star, lock, edit, addTag, saveToDisk, nextScope/previousScope, toggleSidebar, togglePreview (keep it to one scrollable row; hide items that do not fit via `ViewThatFits` or a priority list). It must update live when bindings change (observe `ShortcutManager.shared`).
3. `.help` strings in `PreviewPane`, `SearchBar`, `Sidebar` (read them; only the `.help` literal changes) and the status-bar "Shortcut: ⇧⌘V" line use the live bindings.
4. Global hotkey conflict check: when the user records a global hotkey equal to an in-window binding, show a note in the Shortcuts tab (small addition to `ShortcutsTab.swift`).
5. Tests `Tests/KeyMonitorTests.swift`: a synthetic `NSEvent` for each default binding resolves to the right action; a rebinding (⌘P -> ⌘⇧P) changes resolution; modifier exactness; register in `TestRunner.swift` (suite list only). Extend the 2A behavior harness idea if useful (not required).

## Verify
`scripts/gate.sh` green. Offscreen render of the ActionBar legend before and after rebinding one key (harness in `<scratch>/harness/`). Remove `build/`.

Owns: `Views/History/GlobalKeyMonitor.swift`, `Views/History/ActionBar.swift`, `Views/History/HistoryViewModel.swift` (3E state block), `Views/History/HistoryViewModel+Shortcuts.swift`, `.help` literals in `PreviewPane.swift`/`SearchBar.swift`/`Sidebar.swift`, `Views/StatusBarController.swift` (shortcut label line), `Views/Settings/ShortcutsTab.swift`, `Tests/KeyMonitorTests.swift`, `Tests/TestRunner.swift` (suite list).
Return: summary, the dispatch table, test output, render description, `git status --short`.
