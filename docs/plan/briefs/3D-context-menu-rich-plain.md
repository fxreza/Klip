# Task 3D - Row context menu, rich-text capture, explicit plain-text copy/paste (Sonnet 5)

WORKTREE: (set at launch, branched AFTER wave 1 merged). Do NOT commit. Do NOT touch the main checkout.

Read first: `docs/plan/PLAN.md` (D5 approved: capture RTF/HTML + raw flavors; default paste keeps formatting; explicit "Paste as Plain Text"; setting "Always paste as plain text" off by default), `docs/analysis/clipfield.md` §6 (PasteboardReader / PasteboardFlavors / ClipboardWriter) and port from `reference/clipfield/Sources/Clipfield/Clipboard/{PasteboardReader,PasteboardFlavors,ClipboardWriter}.swift` (MIT, attribute). Ours: `Services/ClipboardWatcher.swift` (text branch; 3C kind assignment; 3F file branch - keep both), `Services/ClipboardStore.swift` (`flavors/` dir, `rtfFilename`, `flavorsFilename` fields exist; `deleteAssociatedFiles` already removes them), `Services/PasteController.swift` (`copyToClipboard`, `paste`, `pasteMultiple`, 3F file branches, 3G AX check), `Views/History/RowContextMenu.swift` (2A: Copy, Pin, Star, Edit, Save, Delete), `Views/PasteButton.swift`, `Views/History/HistoryViewModel.swift` + extensions (`+Lock.swift`: `toggleLockSelection()`; `+Folders.swift`: `moveSelection(toFolder:)`, `keyNewFolder()`; `+Files.swift`: `saveToDisk`, `revealInFinder` if present; `+Shortcuts.swift`: stubs `keyPastePlain/keyCopyPlain` marked `// 3E-stub` that you now implement for real), `Services/ShortcutManager.swift` (`displayString(for:)` for menu key equivalents), `Services/SettingsManager.swift`.

## Deliverables
1. **Rich capture** (`ClipboardWatcher` text branch): alongside the plain string, read `.rtf` data and `.html`; store RTF as `texts/<uuid>.rtf` (`rtfFilename`) when present and **raw flavors** (all pasteboard items' types -> data, binary plist, cap 16 MB total, skip when over) as `flavors/<uuid>.plist` (`flavorsFilename`). Honor `org.nspasteboard.ConcealedType` / `TransientType` / `AutoGeneratedType` by skipping capture (password managers). Kind becomes `.richText` when RTF/HTML exists (3C's detector result is kept for link/email/etc. - only plain `.text` upgrades to `.richText`). Dedup hash unchanged (plain text). Large-text (>50 KB) items: still file-backed plain text; capture flavors only if under the cap.
2. **Write-back** (`PasteController`): `enum PasteMode { case rich, plain }`. `copyToClipboard(item, mode:)`, `paste(item, mode:)`, `pasteMultiple(items, mode:)`: `.rich` replays flavors when `flavorsFilename` exists (restore like Clipfield's `PasteboardFlavors.restore`), else writes RTF + plain, else plain; `.plain` writes only the plain string (for images/files mode is ignored). Multi-paste rich: join plain texts as today (rich multi-join is out of scope - say so in the UI help). Setting `alwaysPastePlain: Bool` (key `paste.alwaysPlain`, default false): when on, default paste/copy use `.plain` and the explicit menu items swap to "Paste with Formatting".
3. **Edit mode**: editing a rich item saves plain text and drops its RTF/flavors (explain in a small note under the editor: "Editing keeps plain text only").
4. **Row context menu** (`RowContextMenu` - full rewrite): Paste, Paste as Plain Text, Copy, Copy as Plain Text, divider, Edit, Pin/Unpin, Star/Unstar, Lock/Unlock (`toggleLockSelection`), Move to Folder > (folders from the store + "Remove from Folder"; `moveSelection(toFolder:)`), Add Tag…, divider, Save to Disk…, Reveal in Finder (file items), Open Link (link items), divider, Delete (disabled + "Locked" suffix when locked). Show key equivalents from `ShortcutManager`. Right-clicking an unselected row selects it first (keep multi-selection when right-clicking inside it).
5. **PasteButton**: split button - main "Paste" (↩) + menu with "Paste as Plain Text" (⌥↩) and the formatting toggle.
6. **View model**: implement `keyPastePlain()`, `keyCopyPlain()` (replace the 3E stubs), `openLink()`, and ensure ⌥↩ in `SearchBar`/monitor reaches `keyPastePlain` (3E part 2 already routes `pastePlain`).
7. Tests `Tests/RichCaptureTests.swift`: flavors bundle round trip (encode/decode, cap), RTF file written/deleted with the item, `PasteMode` selection logic with the setting, concealed-type skip. Register in `TestRunner.swift` (suite list only).

## Verify
`scripts/gate.sh` green. Live: `scripts/run_app.sh`; put RTF on the pasteboard (`osascript -e 'set the clipboard to «data RTF ...»'` is awkward - instead use a tiny Swift script with `NSPasteboard` writing an `NSAttributedString` RTF + plain string); confirm `history.json` has `rtfFilename`/`flavorsFilename` and `kind: richText`; quit. Offscreen render of the context menu is not possible (NSMenu) - render the PasteButton split button and the editor note instead. Remove `build/`.

Owns: `Services/ClipboardWatcher.swift` (text/rich branch), `Services/PasteController.swift` (modes), `Services/PasteboardFlavors.swift` (new), `Services/ClipboardStore.swift` (rtf/flavors file helpers), `Services/SettingsManager.swift` (`alwaysPastePlain`), `Views/History/RowContextMenu.swift`, `Views/PasteButton.swift`, `Views/History/HistoryViewModel+Shortcuts.swift` (replace stubs), `Views/History/HistoryViewModel+Paste.swift` (new), `Views/History/PreviewPane.swift` (editor note only), `Views/SettingsView.swift` (the toggle, General tab), `Tests/RichCaptureTests.swift`, `Tests/TestRunner.swift` (suite list).
Return: summary, API, test output, live verification, `git status --short`.
