# Task 3E (part 1: model, manager, settings UI) - Custom shortcuts (Sonnet 5)

WORKTREE: (set at launch). Do NOT commit. Do NOT touch the main checkout. Do NOT edit `Views/History/**` (task 2A owns it concurrently) - wiring `GlobalKeyMonitor` to the binding table is part 2, done after 2A merges.

Read first: `docs/plan/PLAN.md` (Phase 3 row 3E), `docs/analysis/buffer.md` §3 "Keyboard shortcuts" (the full table of today's in-window keys and keycodes) and change map D, `docs/analysis/clipfield.md` §5 (HotkeyRecorder). Port from `reference/clipfield/Sources/Clipfield/UI/Settings/HotkeyRecorder.swift` (MIT, add attribution). Ours: `Services/SettingsManager.swift` (`HotkeyModifiers`, `keyCodeNames`, global hotkey keys), `Services/HotkeyManager.swift`, `Views/SettingsView.swift` (General tab has the global hotkey recorder + presets today), `Views/History/GlobalKeyMonitor.swift` (read-only: the exact current key handling you must reproduce as defaults).

## Deliverables
1. `Models/KeyBinding.swift`: `struct KeyBinding: Codable, Equatable, Hashable { var keyCode: UInt16; var modifiers: KeyModifiers }` where `KeyModifiers: OptionSet, Codable` {command, shift, option, control}; `var display: String` (glyphs ⌃⌥⇧⌘ + key name, e.g. "⌘⇧V", "⌘⌫", "↩", "⇥", "Esc"); `func matches(_ event: NSEvent) -> Bool` comparing keyCode and the device-independent modifier flags exactly (so ⌘⇧V does not match ⌘V); `static func keyName(keyCode:) -> String` covering letters, digits, F1-F12, arrows, return, tab, space, delete, forward delete, escape, punctuation (extend `keyCodeNames`).
2. `Services/ShortcutManager.swift`: `enum ShortcutAction: String, CaseIterable, Codable` = `paste, pastePlain, copy, copyPlain, delete, pin, star, lock, edit, addTag, saveToDisk, toggleSidebar, togglePreview, nextScope, previousScope, newFolder, renameFolder, moveToFolder, clearFilter, quickPaste1 ... quickPaste9, moveUp, moveDown, extendUp, extendDown, tabComplete, escape` with `label`, `group` (Navigation / Clipboard / Organize / Window), `isRebindable` (arrows/return/escape/tab are fixed = false; show them greyed in the UI), and `defaultBinding` reproducing today's keys exactly: paste ↩, pastePlain ⌥↩, copy ⌘C, copyPlain ⌥⌘C, delete ⌘⌫, pin ⌘P, star ⌘B, lock ⌘L, edit ⌘E, addTag ⌘T, saveToDisk ⌘S, toggleSidebar ⌘⇧S? (pick free combos: toggleSidebar ⌘⌥S, togglePreview ⌘⌥P), nextScope ⌘], previousScope ⌘[, newFolder ⌘N, renameFolder ⌘R, moveToFolder ⌘M, clearFilter ⌫ (only when search empty - note it), quickPasteN ⌘1-9, moveUp ↑, moveDown ↓, extendUp ⇧↑, extendDown ⇧↓, tabComplete ⇥, escape ⎋. `@MainActor final class ShortcutManager: ObservableObject` singleton: `@Published bindings: [ShortcutAction: KeyBinding]` persisted as JSON in UserDefaults key `shortcuts.bindings` (only overrides stored; defaults filled in), `binding(for:)`, `set(_:for:) -> ConflictResult` (refuses a binding already used by another action, returns which), `reset(action:)`, `resetAll()`, `action(for event: NSEvent) -> ShortcutAction?` (first match; the monitor will call this in part 2), `displayString(for:)`. Global hotkey stays in `SettingsManager`/`HotkeyManager` (unchanged) but the Shortcuts tab shows it as the first row using the same recorder.
3. `Views/Settings/HotkeyRecorder.swift`: port Clipfield's recorder (NSViewRepresentable, click to record, Esc cancels, requires at least one of ⌘⌃⌥ for rebindable actions, beeps on rejection, `onRecord(KeyBinding)`), styled with `Theme` tokens.
4. `Views/Settings/ShortcutsTab.swift`: grouped list: "Global" (open Klip = existing global hotkey, using the recorder and writing to `SettingsManager.hotkeyModifiers/hotkeyKeyCode` in the same format as today), then the action groups; each row: label, recorder showing current binding, "Reset" when non-default; conflict -> inline red note "Already used by X"; "Reset All" button. Wire the tab into `Views/SettingsView.swift` (add the tab only; another agent is adding a Permissions tab to the same TabView - keep your diff minimal there). Move the existing global-hotkey recorder/presets out of General into this tab (General keeps a one-line summary "Open Klip: ⌘⇧V - change in Shortcuts").
5. Tests `Tests/ShortcutTests.swift`: defaults table matches the documented keys; display strings; `matches` exactness (⌘V vs ⌘⇧V); conflict detection; persistence round trip via a temporary `UserDefaults(suiteName:)` injected into `ShortcutManager(defaults:)`; reset. Register in `TestRunner.swift` (suite list only).

## Verify
`scripts/gate.sh` green. Offscreen-render the Shortcuts tab (screen is locked) with a scratchpad harness hosting `SettingsView` or just `ShortcutsTab` in an `NSHostingView`, `cacheDisplay` to PNG, Read it. Remove `build/`.

Owns: `Models/KeyBinding.swift`, `Services/ShortcutManager.swift`, `Views/Settings/HotkeyRecorder.swift`, `Views/Settings/ShortcutsTab.swift`, `Views/SettingsView.swift` (tab + moving the hotkey recorder), `Services/SettingsManager.swift` (`keyCodeNames` extension only if needed), `Tests/ShortcutTests.swift`, `Tests/TestRunner.swift` (suite list).
Return: summary, the defaults table as implemented, test output, render description, `git status --short`.
