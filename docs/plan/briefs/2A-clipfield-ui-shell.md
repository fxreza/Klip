# Task 2A - Clipfield-style UI shell (Opus 5)

WORKTREE: (set at launch). Use absolute paths under it. Do NOT commit. Do NOT touch the main checkout.

Read first, in this order: `docs/plan/PLAN.md` (sections 2 decisions, 3 target architecture, 5 Phase 2 row, 7 checklist), `docs/analysis/clipfield.md` (all - this is the design you are replicating), `docs/analysis/buffer.md` section 3 and 4, then the current code: `Views/History/**` (split by task 1A - start with `HistoryViewModel.swift`, `FilterState.swift`, `HistoryContentView.swift`), `Views/Theme/**` (tokens from 1C), `Models/**` (fields from 1B), `Services/SettingsManager.swift`. Then the Clipfield sources you will port from: `reference/clipfield/Sources/Clipfield/UI/Overlay/{OverlayView,OverlayPanel,OverlayController,ClipRowView,ClipPreviewPane,SearchField,PanelResizer}.swift`.

## Goal
Rebuild the history window so it looks and feels like Clipfield (layout, colors, materials, spacing, motion) while keeping **every existing Buffer behavior** (section 7 checklist) and Buffer's click semantics. Use the new model fields so scopes/chips/badges already work; the interactive parts of folders, lock, context menu, file previews and custom shortcuts are later tasks - you provide the surfaces they plug into.

## Window (`Views/History/HistoryPanel.swift`, `HistoryWindowController.swift`)
- `NSPanel` styleMask `[.borderless, .resizable, .nonactivatingPanel]`, `isFloatingPanel`, `level = .floating`, `collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary, .transient]`, `backgroundColor = .clear`, `isOpaque = false`, `hasShadow = true`, `hidesOnDeactivate = false`, `canBecomeKey = true`, `contentMinSize = 560x380`, `contentMaxSize = 1600x1100`. Keep the existing `resignKey -> close` behavior and the `previousApp` capture.
- Size: `SettingsManager.windowWidth/windowHeight` if set else 880x540; persist on resize. Position: centered on the screen under the mouse, 8 % above center (Clipfield `OverlayController.position`). Keep `NSApp.activate(ignoringOtherApps:)` + `makeKeyAndOrderFront` + first responder as today (paste reliability depends on it).
- Content root: `.background(.regularMaterial)` clipped to `RoundedRectangle(cornerRadius: Theme.panelCornerRadius)` with `.strokeBorder(Theme.hairline, lineWidth: 1)`; apply `.preferredColorScheme(SettingsManager.shared.colorScheme.scheme)`. `NSHostingView.sizingOptions = []`.
- Open/close springs like Clipfield (`Theme.promptSpring` on open scale 0.96->1 + opacity; respect Reduce Motion).

## Layout (`HistoryContentView.swift`)
```
HStack(spacing: 0)
  Sidebar (120-320, persisted, collapsible)      | PanelResizer
  VStack: SearchBar / FilterChipBar / Divider / HStack { ClipList | PanelResizer | PreviewPane (200-440, persisted, toggle) } / Divider / ActionBar
```
- **Sidebar** (`Views/History/Sidebar.swift`): title "Klip" 15 bold; rows **All** (`tray.full.fill`), **Favorites** (`star.fill`, count of `isBookmarked`), section header "FOLDERS" (10 bold `.tertiary`, only when folders exist), scrollable folder rows (`folder.fill`, name, count from `store.folderCounts()`), bottom "New Folder" (`plus.circle.fill`) - wire it to `viewModel.requestNewFolder()` which for now shows an inline prompt (`Views/History/FolderPrompts.swift`: name field + Create/Cancel over a `Theme.scrim`, no NSAlert) calling `store.createFolder`. Rename/delete/drag arrive in 3B - leave a `// 3B` marker and a context-menu stub with "Rename…"/"Delete Folder…" disabled. Active row = white text + `Theme.accentGradient` + `matchedGeometryEffect("sidebarSel")`, animated with `Theme.selectionSpring`. Scope enum: `enum Scope: Equatable { case all, favorites, folder(UUID) }` in `FilterState`. ⌘[ / ⌘] cycle scopes. Footer button collapses the sidebar (persist `sidebarCollapsed`).
- **SearchBar** (`SearchBar.swift`): keep Buffer's `TextField`-based search and all its behaviors (debounce, `#tag` mode, tag pill, clear X, item count) but style it like Clipfield (15 pt, magnifier, rounded field). Do not replace the `GlobalKeyMonitor` key routing.
- **FilterChipBar** (`FilterChipBar.swift`): capsule chips **All / Text / Link / Image / File / Color / Code / Email / Phone** (`ContentKind` + `.all`), 11 medium, active = `Theme.accentGradient` + `matchedGeometryEffect("chipSel")`, inactive = `Theme.chipInactive`. Horizontal scroll, no indicators. Filter rule (in `FilterState.apply`): `.all` no-op; `.image` = `type == .image`; `.file` = `isFile`; `.text` = `type == .text && (kind == nil || kind == .text || kind == .richText)`; others = `kind == x`. `kind` is nil for all existing items until 3C backfills - make the chips correct for what exists now.
- **ClipList / ClipRow** (`ClipList.swift`, `ClipRow.swift`; replace `Views/ClipboardListView.swift` and `Views/ClipboardItemRow.swift` - `git rm` them): `LazyVStack(spacing: 3)` inside a `ScrollViewReader`, auto-scroll to selection. Row = 38x38 badge (image thumbnail with `Theme.badgeCornerRadius`; CSS color swatch - keep Buffer's parser, move it to `Views/History/ColorSwatchParser.swift`; file icon via `NSWorkspace.shared.icon(forFile:)` when `fileAttachment.referencePath/stored path` exists else `doc`; otherwise kind icon tinted `Theme.kindTint`) + title (`Font.klip(.rowTitle)` or `.rowTitleMono` for `.code`, 2 lines) + subtitle (source app bold + "·" + `RelativeTimestampView` + up to 2 tag chips) + trailing badges: lock (`lock.fill`), pin (`pin.fill` rotated 40°, orange), star (`star.fill` yellow). Hover `Theme.rowHover`, selected = `Theme.accentGradient` + `Theme.selectionGlow`, multi-selected = same gradient at 0.6 opacity. Row height and badge scale with `CGFloat.klipScaled`. Keep the "Pinned" group header behavior (pinned first).
  **Click semantics (must not change)**: single click = select (with ⌘/⇧ modifiers via the existing `ClickModifierDetector` overlay), double-click = copy + close, no paste on click. Keep `.onHover`. Add `.contextMenu { RowContextMenu(item) }` that for now contains exactly the actions that exist today (Copy, Pin/Unpin, Star/Unstar, Edit, Save Image, Delete) - 3D extends it.
- **PreviewPane** (`PreviewPane.swift`, replaces `DetailPane.swift`): header = kind icon tinted + kind label + existing badges (Large, byte size, multi-select count) + the existing action icon cluster (pin, star, edit, save, OCR, delete) restyled; body = image (fit, max height 240, radius 12, hairline) / color swatch (130 tall + hex) / text (`Font.klip(.preview)`, mono for code, `.textSelection(.enabled)`, keep `ChunkedTextState` loading and the `TextEditor` edit mode) / file placeholder (icon + name + size; 3F replaces with QuickLook) / `MultiSelectionSummary` when several selected; footer metadata rows: Copied (full date), From (source app), Size, Folder (name or "-"), Tags (existing `TagSection` with add-tag input moved here). Empty state `doc.text.magnifyingglass` + "Select an item". Preview pane toggle button in the ActionBar, persisted `showPreviewPane`.
- **ActionBar** (`ActionBar.swift`): left: sidebar toggle + preview toggle; middle: existing shortcut legend (keep text, use `Font.klip(.caption)`; 3E makes it dynamic); right: `PasteButton` (keep). Use `.bar`-like subtle background (`Color.primary.opacity(0.04)`), not a solid control background.
- **Inline prompts**: a reusable `PromptCard` (material + `Theme.promptCornerRadius` + hairline + shadow `.black.opacity(0.3) r20 y8`) over `Theme.scrim`, dismissed by Esc (route through the existing Esc handling chain: edit mode -> tag input -> prompt -> close window).

## Fonts and colors
Every text uses `Font.klip(...)`; every size-coupled frame uses `CGFloat.klipScaled`. No literal `.font(.system(size:))` may remain in `Views/History/**`. No hardcoded colors except via `Theme` (the purple multi-select tint, pin RGB, yellow bookmark, orange badge all become Theme tokens - add what is missing to `Views/Theme/Theme.swift`).

## Keep exactly
All `GlobalKeyMonitor` keys and behaviors; search persistence 90 s; selection restore; edit mode auto-exit; multi-paste; Download All; OCR; pause capture; hide icon; settings window; status bar menu. `HistoryWindowController` public API unchanged (AppDelegate, StatusBarController and the debug hooks call it).

## Verify
1. `scripts/build_local.sh` zero errors, no new warnings; `scripts/run_tests.sh` passes (extend `Tests/FilterStateTests.swift` for scope + chip rules).
2. `scripts/run_app.sh`; `notifyutil -p com.fxreza.klip.debug.show`; copy three things (`echo "https://example.com" | pbcopy`, `echo "#ff6600" | pbcopy`, `echo "plain text $(date)" | pbcopy`); `screencapture -x build/shot-main.png`; Read it. Then screenshot after clicking (use `osascript -e 'tell application "System Events" to click'`? - NO: do not drive the UI with System Events, it needs Accessibility for the terminal). Instead exercise via the view model in a unit test where possible and rely on screenshots for the static look: main view, sidebar collapsed (toggle via a temporary `KLIP_DEBUG` notification `com.fxreza.klip.debug.toggleSidebar` you may add to AppDelegate for testing), preview pane hidden, a selected row (select programmatically on show when `KLIP_DEBUG` and env `KLIP_SELECT_FIRST=1` - small, keep it). Compare against Clipfield: `reference/clipfield` screenshots are in its README (`reference/clipfield/README.md` links) - describe differences honestly.
3. `scripts/run_app.sh --kill`; remove `build/`.

Owns: `Views/History/**`, `Views/ClipboardListView.swift` + `Views/ClipboardItemRow.swift` (deleted), `Views/PasteButton.swift`, `Views/TagChip.swift`, `Views/Theme/Theme.swift` (add tokens only), `Views/ClickDetector.swift`, `AppDelegate.swift` (debug hooks only), `Tests/FilterStateTests.swift`.
Return: summary, file map, what each later task (3A-3H) should hook into (type names, methods), screenshots list, list of any checklist item you could not verify, `git status --short`.
