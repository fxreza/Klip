# v2.5.0-upstream (baseline)

Unmodified upstream source: samirpatil2000/buffer tag `buffer-v2.5.0` (commit d2ca698), built locally on 2026-08-22 with `scripts/build_local.sh --arch both` (universal arm64 + x86_64), signed with the local "QTranslate Dev" identity, not notarized.
Bundle id `com.samirpatil.Buffer`, app name `Buffer`. Official notarized DMGs: https://github.com/samirpatil2000/buffer/releases/tag/buffer-v2.5.0
